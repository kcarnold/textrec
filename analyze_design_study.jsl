trial_all = Open( "/Users/kcarnold/thesis/textrec/data/analyzed/design/trial.csv", Charset( "utf-8" ), );  trial = Subset( trial_all, Rows( trial_all << get rows where( Name( "used-external" ) == "No" ) ) ); Close( trial_all );   // The time before starting to write was not much affected by prompts (even having to respond to the survey query) // when the prompt was in a relevant category. trial << Graph Builder( 	Variables( X( :category_is_relevant ), Y( :secsBeforeStartTyping ), Overlay( :conditionName ) ), 	Elements( Bar( X, Y, Legend( 10 ), Error Bars( "Standard Error" ) ) ), 	Local Data Filter( Add Filter( columns( :exclude_from_timing ), Where( :exclude_from_timing == "False" ), ) ) );   // FIXME: is this the correct random effects structure for this mixed model? trial << Fit Model( 	Y( :secsBeforeStartTyping ), 	Where( :exclude_from_timing == "False" ), 	Effects( :category_is_relevant, :conditionName, :prompt, :category_is_relevant * :conditionName ), 	Random Effects( :pid, :cueId, :pid * :conditionName, :cueId * :conditionName ), 	NoBounds( 1 ), 	Personality( "Standard Least Squares" ), 	Method( "REML" ), 	Emphasis( "Minimal Report" ), 	Run( 		:secsBeforeStartTyping << {Summary of Fit( 1 ), Analysis of Variance( 0 ), Parameter Estimates( 1 ), 		Lack of Fit( 0 ), Show Prediction Expression( 1 ), Scaled Estimates( 0 ), Plot Actual by Predicted( 0 ), 		Plot Regression( 0 ), Plot Residual by Predicted( 0 ), Plot Studentized Residuals( 0 ), 		Plot Effect Leverage( 0 ), Plot Residual by Normal Quantiles( 0 ), Show All Confidence Intervals( 1 )} 	) );   trial << Graph Builder( 	Variables( X( :category_is_relevant ), Y( :text_len ), Overlay( :conditionName ) ), 	Elements( Bar( X, Y, Legend( 10 ), Error Bars( "Standard Error" ) ) ) );  trial << Fit Model( 	Y( :text_len ), 	Effects( 		:category_is_relevant, 		:conditionName, 		:prompt, 		:category_is_relevant * :conditionName, 		// TODO: I notice this interaction on the graph: verbatim is good for travel guide when relevant. 		:category_is_relevant * :conditionName * :prompt, 		:trialIdx[:prompt] 	), 	Random Effects( :pid, :cueId, :pid * :conditionName, :cueId * :conditionName ), 	NoBounds( 1 ), 	Personality( "Standard Least Squares" ), 	Method( "REML" ), 	Emphasis( "Minimal Report" ), 	Run( 		:text_len << {Summary of Fit( 1 ), Analysis of Variance( 0 ), Parameter Estimates( 1 ), Lack of Fit( 0 ), 		Show Prediction Expression( 1 ), Scaled Estimates( 0 ), Plot Actual by Predicted( 0 ), 		Plot Regression( 0 ), Plot Residual by Predicted( 0 ), Plot Studentized Residuals( 0 ), 		Plot Effect Leverage( 0 ), Plot Residual by Normal Quantiles( 0 ), Show All Confidence Intervals( 1 )} 	) );  block_all = Open( "/Users/kcarnold/thesis/textrec/data/analyzed/design/block.csv", Charset( "utf-8" ), ); block = Subset( block_all, Rows( block_all << get rows where( Name( "used-external" ) == "No" ) ) ); Close( block_all );  block << Graph Builder( 	Where(:conditionName != "noprompt"), 	Variables( X( :category_is_relevant ), Y( :n_relevant ), Overlay( :conditionName ) ), 	Elements( Box Plot( X, Y, Legend( 7 ) ) ) );  block << Fit Model( 	Y( :n_relevant ), 	Effects( 		:category_is_relevant, 		:conditionName, 		:prompt, 		:category_is_relevant * :conditionName 	), 	Random Effects( :pid ), 	NoBounds( 1 ), 	Personality( "Standard Least Squares" ), 	Method( "REML" ), 	Emphasis( "Minimal Report" ), 	Run( 		:n_relevant << {Summary of Fit( 1 ), Analysis of Variance( 0 ), 		Parameter Estimates( 1 ), Lack of Fit( 0 ), Show Prediction Expression( 1 ), 		Scaled Estimates( 0 ), Plot Actual by Predicted( 0 ), Plot Regression( 0 ), 		Plot Residual by Predicted( 0 ), Plot Studentized Residuals( 0 ), 		Plot Effect Leverage( 1 ), Plot Residual by Normal Quantiles( 1 ), 		Show All Confidence Intervals( 1 )} 	), 	SendToReport( 		Dispatch( 			{"Response n_relevant"}, 			"Effect Details", 			OutlineBox, 			{Close( 0 )} 		) 	) );  /** * Questions:  - Did relevance depend on the category alignment? - Did amount written depend on experience? */  /// Trial-level:  // Time to start writing  // Amount written  // ;